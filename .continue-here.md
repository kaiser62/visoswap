# Continue here

*Paused 2026-08-21. Working tree clean — nothing half-written.*

## Position

Phase 1 of 6, "Vendor the Engine & Strip Qt". Plans 01-01 and 01-02 complete.
Plan 01-03 is **half done**: Task 1 committed, Task 2 not started.

| Plan | State |
|---|---|
| 01-01 package skeleton, GPLv3, Qt gate | complete, verified |
| 01-02 vendor the Qt-free bulk | complete, verified |
| 01-03 context surface + de-Qt `models_processor.py` | Task 1 done (`2bf871d`), **Task 2 not started** |
| 01-04 de-Qt `frame_worker.py`, close the phase | not started |

Last commit: `2bf871d feat(01-03): enumerate the main_window surface and define EngineContext`.
16 tests pass. `git status` clean.

## Resume with

Plan 01-03 **Task 2**: de-Qt `visoswap/processors/models_processor.py`.

That file is not vendored yet. Task 2 vendors it *and* strips Qt in one step:
drop the `QtCore` import, the `QObject` base class, both Signals, the 8
`model_loading_signal` / `model_loaded_signal` emissions, and the 3
`model_load_dialog` accesses.

Read `01-03-PLAN.md` Task 2 for the exact steps, and `01-CONTEXT-SURFACE.md`
for the attribute mapping Task 1 produced.

## What exists

- 26 vendored modules under `visoswap/`, all probed Qt-clean
- `visoswap/processors/context.py` — `EngineContext`, 7 fields, each documented
  with the upstream `main_window.<attr>` read it replaces
- `.planning/phases/01-vendor-the-engine-strip-qt/01-CONTEXT-SURFACE.md`
- Two independent gates in `tests/`: a subprocess import probe blocking seven Qt
  roots, and a `tokenize`-based static source scan. Both discover their own
  coverage from `PENDING_QT_STRIP` in `tests/conftest.py`.

## Decisions that constrain the next session

- **`PENDING_QT_STRIP` in `tests/conftest.py` still holds both
  `visoswap.processors.models_processor` and
  `visoswap.processors.workers.frame_worker`.** Remove `models_processor` when
  Task 2 makes it clean; `frame_worker` comes out in 01-04. Emptying the set is
  what closes the phase.
- **Vendored bodies are byte-identical to upstream below the attribution
  header.** `models_processor.py` is the first file where real edits are
  intended — record exactly what changed so it stays diffable against upstream.
- **Tests fail, never skip, on missing engine deps.** A skip is a false green on
  this phase's only success criterion.
- **Default test interpreter** is `D:/Visomaster/dependencies/Python/python.exe`
  (has torch/onnxruntime/tensorrt *and* PySide6 6.7.2), overridable via
  `VISOSWAP_ENGINE_PYTHON`. Blocking Qt where Qt is installed is the stronger
  proof.
- **Deleting the model-load signalling** removes the engine's only load-progress
  reporting. The re-add path must be recorded in `01-CONTEXT-SURFACE.md` so a
  later phase finds a decision rather than an absence.
- **Deferred on purpose, do not "fix" in passing:** `models_dir =
  './model_assets'` stays hardcoded (Phase 4 makes it env-driven);
  `clipseg.py:305`'s `torch.load` stays as-is (Phase 2 adds `weights_only=True`).

## Blockers and traps

- **Plan line numbers run one low.** The planner counted content lines; files
  end with a trailing newline. Verify the 8 emission sites (128, 142, 149, 163,
  170, 181, 280, 283) and 3 dialog accesses (215, 218, 219) against the real
  file before editing.
- **01-04's clean-room check needs its own venv.** The default test interpreter
  lives inside `D:/Visomaster/dependencies/Python/` — the tree being vendored
  away from — so it cannot prove Qt is genuinely absent. Build a venv from
  `requirements-engine.txt`.
- **01-04 Task 3 is a blocking human checkpoint.** The phase will not
  self-certify; it stops and wants a person.
- **`D:/Visomaster/.git/config` is malformed** (`bad config line 1`,
  pre-existing). Any `git -C` call there fails. Do not repair it — it is
  read-only source.
- **DFM, LivePortrait and CLIPseg are import-proven only.** Nothing in Phase 1
  executes them. Phase 2 owns that risk; do not let a summary imply otherwise.

## Source material — read-only, never modify

- `D:/Visomaster` — the code being vendored
- `C:/Users/Sakat/.devswarm/repos/1/ef7f9c3d/config-parallel-setup` — the
  working `visomaster_live` app being carried over

## Open elsewhere

`test_recorder.py::test_a_cancelled_run_leaves_a_playable_partial` hangs
indefinitely in `visomaster_live`. Pre-existing on master, currently deselected.
Phase 4 is scheduled to fix it rather than carry the deselect forward.
