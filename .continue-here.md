# Continue here

*Paused 2026-08-21, second pause. Phase 1 is one human checkpoint away from done.*

## Position

Phase 1 of 6, "Vendor the Engine & Strip Qt". Plans 01-01, 01-02, 01-03
complete. Plan **01-04 is two-thirds done**: Tasks 1 and 2 committed, Task 3 —
the blocking human checkpoint — not started. No SUMMARY written yet.

Phase 2 is fully planned (4 plans) but **not checker-reviewed** and not started.

| Plan | State |
|---|---|
| 01-01 skeleton, GPLv3, Qt gate | complete, verified |
| 01-02 vendor the Qt-free bulk | complete, verified |
| 01-03 context surface + de-Qt `models_processor` | complete, verified |
| 01-04 de-Qt `frame_worker`, close the phase | Tasks 1-2 done, **Task 3 pending**, no SUMMARY |
| 02-01…02-04 | planned, **not checked**, not started |

HEAD `208e8f0`. **25 tests pass.** Working tree has two uncommitted items, both
legitimate work from the interrupted Task 2 — see below.

## Verified right now, independently

- `frame_worker.py` is vendored and carries **zero** references to
  `main_window`, `QtCore`, `PySide`, `QObject`, `isChecked`,
  `get_pixmap_from_frame`, or `video_processor`.
- `PENDING_QT_STRIP` is now `frozenset()` — **empty**. That was the phase's
  closing condition.
- All **30** vendored modules probe `CLEAN` (exit 0) under the seven-root Qt
  block.

## Uncommitted, and why

- `.gitignore` — adds `.venv-clean/`, for the Task 3 clean-room venv.
- `docs/verifying-qt-free.md` — untracked, ~140 lines, explains why the two
  verification methods differ. Task 3 depends on it.

Both belong to Task 3's setup. Commit them with Task 3 rather than separately.

## Resume with

Plan 01-04 **Task 3** — the blocking human checkpoint.

Build a venv from `requirements-engine.txt` and confirm the engine imports where
Qt is **genuinely absent**, not merely blocked. The default test interpreter
(`D:/Visomaster/dependencies/Python/python.exe`) has PySide6 6.7.2 installed, so
it can only ever prove "blocked", never "absent". That gap is the entire reason
this checkpoint exists and it must not be self-certified by an agent.

Then write `01-04-SUMMARY.md` and close the phase.

## Decisions that constrain the next session

- **DFM and CLIPseg are descoped**, decided by the project owner 2026-08-21. See
  `.planning/phases/02-engine-api-first-swap/02-DECISION-deferred-paths.md`.
  Both stay vendored and import-proven; neither gets exercised. **Not** removed —
  re-adding must not mean redoing the vendoring. This answers plan 02-04's
  `checkpoint:decision` and collapses that plan to documentation.
- **LivePortrait is still exercised for real** in plan 02-03. It is the one
  deferred path with weights present.
- Deferred on purpose: `models_dir = './model_assets'` stays hardcoded until
  Phase 4; `clipseg.py:305`'s `torch.load` gets `weights_only=True` in Phase 2.
- Vendored bodies stay byte-identical to upstream below the attribution header,
  except at the named edit sites. That property is what keeps the tree diffable
  against upstream.
- Tests fail, never skip, on missing engine deps. A skip is a false green.

## Blockers and traps

- **Phase 2's plans have not been through `gsd-plan-checker`.** Phases 1's plans
  were. Run it before executing 02-01.
- **The roadmap needs two corrections.** Phase 2's `**Plans**: TBD` line was left
  untouched deliberately (Phase 1 was committing concurrently) and needs setting
  to 4 plans. Phase 2's success criterion "exercises DFM, LivePortrait and
  CLIPseg at least once each" must be amended to LivePortrait only.
- **Phase 4's "54 tracked files" criterion is wrong** — `models_list` plus
  `models_trt_list` is **62** entries. Re-derive rather than trust it.
- **3 of the 4 `*_layout_data` modules reach PySide6**, not just
  `settings_layout_data` as the design doc says. Only `face_editor_layout_data`
  is clean. The Phase 3 settings fixture must therefore be generated offline.
- **`self.context` is not unique in the tree.** `utils/tensorrt_predictor.py`
  uses it for a TensorRT execution context. Do not blanket-rewrite it.
- **`ModelsProcessor` defaults to TensorRT**, whose EP options write to a
  *relative* `tensorrt-engines/` directory — on a wrong working directory that
  writes into the read-only source tree. Phase 2 locks to CUDA.
- **`D:/Visomaster/.git/config` is malformed** (`bad config line 1`),
  pre-existing. Any `git -C` call there fails. Do not repair it.
- **Agents have twice written scratch files into the working repo** (stray `'`,
  `205`, `from`, `out.txt` from redirect mishaps). Cleaned. Check
  `git status --short` in both source repos after any agent run.

## Phase 3 groundwork already done

A throwaway prototype and full type taxonomy for the settings schema live in
the scratchpad at
`D:/Dev/tmp/claude/C--Users-Sakat--devswarm-repos-1-ef7f9c3d-config-parallel-setup/a72e48bc-b4c0-47a4-b3ab-f98701283bde/scratchpad/schema/`.
Findings are in ruflo memory, namespace `visoswap`, key
`visoswap-schema-taxonomy`. Headlines: type must be derived from **widget
shape**, never the key name (`ClipText` is a text field that the name convention
renders as a 0-1000 slider); defaults are 157 str / 43 bool / **zero numeric**;
gating has **two** mechanisms (`parentToggle` and an undocumented
`parentSelection`) and is transitive; dropping `exec_function` makes
`ProvidersPrioritySelection`, `nThreadsSlider` and `VideoPlaybackCustomFpsToggle`
inert, so each needs an apply-handler.

## Source material — read-only, never modify

- `D:/Visomaster`
- `C:/Users/Sakat/.devswarm/repos/1/ef7f9c3d/config-parallel-setup`

## Open elsewhere

`test_recorder.py::test_a_cancelled_run_leaves_a_playable_partial` hangs
indefinitely in `visomaster_live`. Pre-existing, currently deselected. Phase 4
fixes it rather than carrying the deselect forward.
