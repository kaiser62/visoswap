{
  "version": "1.0",
  "timestamp": "2026-08-21T14:59:33.879Z",
  "phase": "2",
  "phase_name": "Engine API & First Swap",
  "phase_dir": ".planning/phases/02-engine-api-first-swap",
  "plan": 1,
  "task": 3,
  "total_tasks": 3,
  "status": "paused",
  "pause_point": "clean plan boundary for MY work (02-01 complete, committed, verified) -- BUT a concurrent agent has since committed feat(02-02) and had uncommitted work in flight at the moment this was written",
  "completed_tasks": [
    {
      "id": 1,
      "name": "Make the 12GB weight set reachable without copying it",
      "status": "done",
      "commit": "fa0fc8e"
    },
    {
      "id": 2,
      "name": "Extract typed settings once, offline, and pin the result (TDD)",
      "status": "done",
      "commit": "08171e8 (RED) + 1b70d6c (GREEN)"
    },
    {
      "id": 3,
      "name": "Seal the runner against Qt, VisoMaster and the backend",
      "status": "done",
      "commit": "6a1c9f0"
    },
    {
      "id": 4,
      "name": "SUMMARY, STATE, ROADMAP",
      "status": "done",
      "commit": "58ffd30"
    }
  ],
  "remaining_tasks": [
    {
      "id": 5,
      "name": "02-02-PLAN.md -- CUDA provider pin + ModelsProcessor in the sealed runner",
      "status": "committed_by_another_agent",
      "commit": "40dfb90",
      "note": "not executed or reviewed by me"
    },
    {
      "id": 6,
      "name": "02-03-PLAN.md -- Engine.detect_faces / Engine.swap on the self.frame seam; LivePortrait",
      "status": "unknown",
      "note": "another agent may be mid-execution; tests/artifacts/ holds swapped-frame PNGs"
    },
    {
      "id": 7,
      "name": "02-04-PLAN.md -- documentation only; DFM and CLIPseg gaps. ENGINE-01 gets marked complete HERE.",
      "status": "not_started"
    }
  ],
  "blockers": [],
  "async_jobs": [],
  "human_actions_pending": [],
  "decisions": [
    {
      "decision": "Five widget shapes exist in the layout dicts, not four; 93 int keys, not 94",
      "rationale": "ClipText carries min/max with no step — character bounds on a line edit, default ''. The planned 'min/max without decimals -> int' rule does not mistype it, it crashes the generator on int(float('')). 'step' is the slider signal.",
      "phase": "2"
    },
    {
      "decision": "Seal block lists live in tests/_blocked_roots.py (import-free), re-exported by conftest.py",
      "rationale": "Both subprocess consumers run on the engine interpreter, which has no pytest (measured on the portable python and on .venv-clean), so neither can import conftest. One definition, two import sites — and one fewer copy than before, since _qt_guard_probe.py's private list was deleted.",
      "phase": "2"
    },
    {
      "decision": "The runner appends the VisoMaster checkout to sys.path BEFORE arming the seal",
      "rationale": "A meta_path finder raises on the name, so a self-test passes identically against a package that never existed. Plan 01-04's lesson: an inert blocker reports the same CLEAN as a working one. reachable_before_seal= makes inertness visible; backend=no is stated rather than hidden.",
      "phase": "2"
    },
    {
      "decision": "The backend import scan uses ast, not tokenize comment-stripping",
      "rationale": "The question is 'is this an import statement', which the grammar answers exactly, so a removal note mentioning backend is a non-hit by construction rather than by filtering.",
      "phase": "2"
    },
    {
      "decision": "Reverted requirements.mark-complete ENGINE-01",
      "rationale": "ENGINE-01 is 'swap a frame with no PySide6 installed'. No frame has been swapped — 02-01 builds the harness only. The tool trusts the plan's frontmatter, which names the requirement the PHASE serves, not one the PLAN completes.",
      "phase": "2"
    }
  ],
  "uncommitted_files": [],
  "next_action": "Resolve the concurrency first, not a plan. Run `git log --oneline -15` and `git status --short` to establish real state. If the other agent's work is still uncommitted, leave it alone. Then re-verify the harness: python tools/link_model_assets.py --check; python -m pytest tests/ -q; \"D:/Visomaster/dependencies/Python/python.exe\" -B tests/_engine_runner.py --selftest (expect a CLEAN line containing app=yes). tests/_engine_runner.py has been modified since 02-01 -- re-read it. Do NOT mark ENGINE-01 complete before 02-04.",
  "context_notes": "Paused at a clean boundary, not mid-task. 45 tests pass on both the Qt-bearing portable interpreter and the Qt-free .venv-clean. The working tree is clean except for .planning/phases/03-settings-schema-three-tier-resolution/, which ANOTHER PROCESS wrote at 20:41-20:42 during this session and which I deliberately left uncommitted — there is a concurrent planner writing into this repo. 02-02's first job is pinning the ONNX Runtime provider to CUDA: ModelsProcessor defaults to TensorRT, whose provider options write a cache to a RELATIVE tensorrt-engines/ directory, and D:/Visomaster/tensorrt-engines/ already contains a nested tensorrt-engines/tensorrt-engines/ from an earlier wrong-CWD run. That hazard is real and already visible on disk. CORRECTION AFTER WRITING: a second agent is executing in this same working tree. The '45 passed' and 'clean tree' figures above describe the moment 02-01 closed and were already stale by the time this was committed. Everything I assert is scoped to 02-01; I did not execute or review 02-02.",
  "concurrency_warning": {
    "another_agent_active": true,
    "observed_commits_not_mine": [
      "b998095 docs(03): plan Phase 3",
      "adc470e docs: record Phase 3's plan count",
      "40dfb90 feat(02-02): publish Engine and FaceCard"
    ],
    "uncommitted_at_pause_not_mine": [
      "tests/_engine_runner.py (+299 lines)",
      "tests/artifacts/smoke_source_frame.png",
      "tests/artifacts/smoke_swapped_frame.png"
    ],
    "instruction": "Do NOT git clean / checkout -- / stash / reset in this tree. That work is live, not stale. Reconcile with the other agent before acting."
  }
}
