---
phase: 01-vendor-the-engine-strip-qt
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - visoswap/processors/workers/__init__.py
  - visoswap/processors/workers/frame_worker.py
  - visoswap/processors/context.py
  - tests/test_qt_free.py
  - tests/test_no_qt_source.py
  - tests/test_dropped_modules.py
  - docs/verifying-qt-free.md
autonomous: false
requirements: [LICENSE-01]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every module under visoswap/ imports with all Qt binding roots blocked, and no blocked root lands in sys.modules."
    - "visoswap/processors/ contains no video_processor module — dropped whole, not stubbed."
    - "frame_worker.py reaches no app/ui action module and reads the two former button states as plain booleans on EngineContext."
    - "A human has confirmed the same import in a virtualenv where PySide6 is genuinely not installed."
  artifacts:
    - visoswap/processors/workers/frame_worker.py
    - tests/test_dropped_modules.py
    - docs/verifying-qt-free.md
  key_links:
    - "tests/test_qt_free.py PENDING_QT_STRIP -> empty: this set is the only thing standing between a green suite and an unproven module, so an empty set is the phase's actual pass condition."
    - "frame_worker.run() -> self.frame: with the display path deleted, the processed frame has no exit route until Phase 2's Engine.swap() defines one; this is the seam between the two phases."
---

<!-- planner-discipline-allow: PySide6, PySide2, PyQt5, PyQt6, qtpy, shiboken6, shiboken2, QtCore, QtGui, QtWidgets, QObject, QPixmap, Signal, Slot, app.ui, main_window, swapfacesButton, editFacesButton, isChecked, video_processor, get_pixmap_from_frame, update_parameters_and_control_from_marker -->

<objective>
Strip Qt from `workers/frame_worker.py` — the 1,306-line swap pipeline and the last coupled file —
then close the phase: empty the pending-exclusion set, prove every vendored module imports with all
Qt binding roots blocked, and confirm the result in a real virtualenv where Qt is absent rather
than merely blocked.

Purpose: this is where the phase's success criterion becomes true. Everything before it was
scaffolding for this gate.

Output: a Qt-free `frame_worker.py`, an empty `PENDING_QT_STRIP`, a passing full-tree import gate,
and `docs/verifying-qt-free.md` recording how the clean-room check is run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@docs/specs/2026-08-21-visoswap-standalone-design.md
@.planning/phases/01-vendor-the-engine-strip-qt/01-CONTEXT-SURFACE.md
@.planning/phases/01-vendor-the-engine-strip-qt/01-03-SUMMARY.md
</context>

<source_boundary>
`D:/Visomaster` is **read-only source material**. Never write to anything under it. Its portable
interpreter at `D:/Visomaster/dependencies/Python/python.exe` may be *executed*, which writes no
files into that tree beyond ordinary bytecode caching — do not pass it a script located inside
`D:/Visomaster`.
</source_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Strip Qt from frame_worker.py</name>
  <precondition>`visoswap/processors/context.py` and `visoswap/processors/models_processor.py` exist from plan 03, and `visoswap/processors/utils/misc.py` exports `t512`, `t384`, `t256`, `t128`.</precondition>
  <files>visoswap/processors/workers/__init__.py, visoswap/processors/workers/frame_worker.py, visoswap/processors/context.py</files>
  <action>
Copy `D:/Visomaster/app/processors/workers/frame_worker.py` (1,306 lines) to
`visoswap/processors/workers/frame_worker.py`, add an import-free
`visoswap/processors/workers/__init__.py`, then apply the plan-01 header, the plan-02 import
rewrite map, and the changes below. Upstream line numbers were measured; check each against the
copy before editing, since earlier deletions shift everything after them — work from the bottom of
the file upward, or re-locate by content rather than by number.

**The two `app/ui` imports, lines 16-17.** Delete both. `common_actions` and `video_control_actions`
pull in a Qt binding transitively, and — measured — the chain reaches Qt through `qtpy`, not only
through a direct binding import, which is why the gate blocks every binding root rather than one.
This file calls exactly two functions between them, and both are on paths this project drops:
`get_pixmap_from_frame` at line 57 is the display path, and
`update_parameters_and_control_from_marker` at line 43 is timeline markers. Deleting the imports is
correct precisely because both call sites go too.

**Line 18** rewrites to `visoswap.processors.utils.misc` for the four scaling transforms. **Lines
20-21**, the `if TYPE_CHECKING:` import of the host window, becomes an `EngineContext` import.

**Constructor, line 26.** Replace the `main_window` parameter with `context: 'EngineContext'`.
Delete line 33, `self.video_processor = main_window.video_processor` — that module is dropped whole
and no attribute of it survives. Point line 32 (`models_processor`) and line 36 (`target_faces`) at
`self.context`. The `frame_queue` constructor parameter and the `self.frame_queue` assignment at
line 28 exist only to feed the display path: grep for every use of `self.frame_queue` in the copy,
and if the only ones are inside the block deleted below, remove the parameter and the assignment
too. If any use survives outside that block, keep both and say which use in the SUMMARY.

**Marker update, lines 42-43.** Delete both lines together, not just line 43. The `with
models_processor.model_lock:` at line 42 guards nothing except the marker call, so removing the
call alone leaves an empty `with` block. Note that line 44 — `self.parameters =
context.parameters.copy()` after rewriting — sits *outside* that block and must survive.

**Button reads, five lines: 48, 137, 163, 170, 183.** Each reads `swapfacesButton.isChecked()` or
`editFacesButton.isChecked()` as a plain boolean and nothing more. Replace each with the
corresponding `EngineContext` flag, `swap_faces_enabled` or `edit_faces_enabled`, per
`01-CONTEXT-SURFACE.md`. Line 48 reads both flags plus
`control['FrameEnhancerEnableToggle']`; line 163 reads both. The design's inspection listed four
such lines; measurement found five — line 183 reads `editFacesButton` as well. Confirm the count
against the copy and record what you find in the SUMMARY.

**Display path, lines 55-78.** Delete the whole block: the `get_pixmap_from_frame` call at 57, the
three signal emissions at roughly 60-70 that branch on webcam versus video versus single frame, the
frame-queue `get()` and `task_done()` at 73-74, and the playback-completion check calling
`stop_processing()` at 77-78. Every one of these reaches the dropped playback loop. Line 51,
`self.frame = np.ascontiguousarray(self.frame)`, is *before* the block and must survive — it is
what leaves the processed result on the instance.

With the emissions gone, `run()` finishes with the result on `self.frame` and no route out. That is
the intended seam, not an oversight: Phase 2's `Engine.swap(frame_number, source, settings)` defines
how a caller retrieves it. Leave `self.frame` as the carrier and say so in the SUMMARY. Do not
invent a queue, a callback, or a return protocol here — Phase 2 owns that decision, and guessing at
it now means Phase 2 either inherits a wrong guess or spends its budget undoing one.

**Remaining host-window reads.** `target_faces` at lines 85, 160, 241 and 292, and
`control.copy()` at line 129, become reads on `self.context`. After this, no `main_window`
reference should remain anywhere in the file.

Everything else in the file — the swap, restore, mask, editor and enhancer logic that makes up the
other ~1,200 lines — is untouched. Do not reformat and do not refactor. The Qt coupling here is
small and localised; that is the design's central bet, and a surgical diff is what proves it.
  </action>
  <verify>
    <automated>cd D:/Dev/visoswap &amp;&amp; "D:/Visomaster/dependencies/Python/python.exe" tests/_qt_guard_probe.py visoswap.processors.workers.frame_worker | grep -qx CLEAN</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; python - &lt;&lt;'PY'
import io, re, sys, tokenize
p = "visoswap/processors/workers/frame_worker.py"
src = open(p, encoding="utf-8").read()
out, last = [], (1, 0)
for tok in tokenize.generate_tokens(io.StringIO(src).readline):
    if tok.type != tokenize.COMMENT:
        out.append((tok.start[0], tok.string))
bad = [f"{p}:{ln}: {s}" for ln, s in out if re.search(r"main_window|isChecked|video_processor|get_pixmap_from_frame|update_parameters_and_control_from_marker", s)]
print("\n".join(bad) or "clean")
sys.exit(1 if bad else 0)
PY</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
`frame_worker.py` imports with every Qt binding root blocked. A comment-stripped token scan finds
no host-window reference, no button state read, no playback-loop reference, and neither of the two
former `app/ui` call sites. The processed frame is left on `self.frame`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Close the gate on the whole tree</name>
  <files>tests/test_qt_free.py, tests/test_no_qt_source.py, tests/test_dropped_modules.py</files>
  <action>
Empty the `PENDING_QT_STRIP` set in both `tests/test_qt_free.py` and `tests/test_no_qt_source.py`,
leaving the constant in place with a comment recording that Phase 1 closed it. An empty set is the
phase's actual pass condition — every prior plan added coverage, and this is where nothing is
excluded from it.

Make the import gate cover the package as well as its parts. `visoswap/processors/__init__.py` is
deliberately import-free, so `import visoswap.processors` on its own would exit 0 while proving
nothing — it would import an empty file. Keep `__init__.py` import-free, because eagerly importing
the tensorrt- and torch-heavy modules on package import is the wrong default for a library, and
instead have the test hand the probe the package name **plus** every discovered submodule in one
run. Assert exit code 0 and `CLEAN`. Record in a comment why the package name alone is insufficient,
so nobody later "simplifies" the test into a hollow pass.

Add `tests/test_dropped_modules.py` asserting that nothing named `video_processor` exists anywhere
under `visoswap/` — no module, no stub, no re-export. The roadmap requires the file be dropped
whole rather than stubbed, and a stub is exactly what a future contributor would add on hitting an
import error. Assert on the filesystem, not on an import attempt, since a missing module and a
broken module both raise.

Also assert in the same file that no module under `visoswap/` imports anything from the `backend`
package. That is Phase 2's criterion 3, but the check costs one line here and catches an accidental
coupling at the moment it is introduced rather than a phase later.
  </action>
  <verify>
    <automated>cd D:/Dev/visoswap &amp;&amp; python -m pytest tests/ -q</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; ! find visoswap -iname "*video_processor*" | grep -q .</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; ! grep -rEn "^\s*(from|import)\s+(backend|app)\b" --include="*.py" visoswap/ | grep -q .</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; grep -rn "PENDING_QT_STRIP" tests/ | grep -qE "PENDING_QT_STRIP[^=]*=\s*(set\(\)|frozenset\(\)|\{\s*\})"</automated>
  </verify>
  <done>
`pytest tests/ -q` passes with `PENDING_QT_STRIP` empty. The import gate covers the package name
and every submodule in one probe run. No `video_processor` artefact exists under `visoswap/` and no
vendored module imports `backend` or `app`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the import in a virtualenv with no Qt installed</name>
  <what-built>
Every module under `visoswap/` now imports with all Qt binding roots blocked, verified
automatically on VisoMaster's portable interpreter — which has PySide6 6.7.2 installed, so the
block is doing real work rather than confirming an absence.

The roadmap's criterion 1 asks for something adjacent but distinct: the same import succeeding in a
virtualenv where `pip show PySide6` reports nothing installed. That is a genuinely different
failure mode — a stale `.pth`, a namespace package, or a conditional import guarded on
availability could pass the blocked-import gate and still fail on a machine with no Qt at all. It
is left as a human check rather than a test because building the environment pulls multi-gigabyte
CUDA-pinned torch, torchvision and tensorrt wheels, which is not something every test run should do.

Before requesting this checkpoint, write `docs/verifying-qt-free.md` with the exact commands below
so the check is repeatable in Phase 6 rather than reconstructed.
  </what-built>
  <how-to-verify>
1. Create a fresh virtualenv on Python 3.10 (the vendored code targets 3.10; the repo's default
   interpreter is 3.13 and uses `pkg_resources`, which is removed there):
   `py -3.10 -m venv D:/Dev/visoswap/.venv-clean`

2. Install the engine requirements, which carry no Qt entry:
   `D:/Dev/visoswap/.venv-clean/Scripts/pip install -r D:/Dev/visoswap/requirements-engine.txt`
   Expect a long download. If a CUDA-pinned wheel is unavailable for your setup, note it and use
   the CPU wheels — this check is about import reachability, not inference.

3. Confirm Qt is genuinely absent, which is the whole point of the exercise:
   `D:/Dev/visoswap/.venv-clean/Scripts/pip show PySide6`
   Expect `WARNING: Package(s) not found: PySide6`.

4. Import the tree with that interpreter:
   `cd D:/Dev/visoswap && .venv-clean/Scripts/python tests/_qt_guard_probe.py $(...every module...)`
   Simplest path: run `.venv-clean/Scripts/python -m pytest tests/ -q` with
   `VISOSWAP_ENGINE_PYTHON` pointed at that same interpreter.
   Expect `CLEAN` and a green suite.

Report back either "approved" plus the `pip show` output, or the first module that failed together
with its traceback. A failure here is a real finding, not a checkpoint formality — it means
something reaches Qt in a way the block did not model.
  </how-to-verify>
  <resume-signal>Type "approved" or paste the failing module and traceback</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendored engine -> host application | After this plan, `visoswap/` is a library with no host window. `EngineContext` is the only inbound channel. |
| Test gate -> phase completion claim | The gate is the sole evidence for the phase's success criterion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-13 | Spoofing | `import visoswap.processors` as a success criterion | high | mitigate | With an import-free `__init__.py`, the roadmap's literal command passes while importing nothing. The gate hands the probe the package name plus every discovered submodule, and a comment in the test records why the package name alone is insufficient. |
| T-01-14 | Spoofing | Blocked-import gate vs. real absence | medium | mitigate | A blocker models one failure mode; a machine with no Qt installed is another. Both are covered — the block runs automatically every test run, and the clean virtualenv is a blocking human checkpoint with its commands recorded in `docs/verifying-qt-free.md`. |
| T-01-15 | Denial of service | Deleted playback-completion and queue draining in `frame_worker.run()` | medium | accept | Upstream drained `frame_queue` and called `stop_processing()` on completion; both are deleted with the playback loop. The scheduler already owns playback and queue lifecycle, so re-implementing them here would produce the two-schedulers problem PROJECT.md rules out. Accepted with the constraint recorded: Phase 2's `Engine.swap()` must define the result-retrieval and completion protocol, or worker threads accumulate. |
| T-01-16 | Tampering | `.venv-clean` creation in the human checkpoint | high | mitigate | This is the phase's only package-manager install and the first point where the Package Legitimacy Gate applies. `requirements-engine.txt` introduces no package that VisoMaster did not already pin upstream; the human performing the checkpoint reviews the resolved install list before proceeding and reports anything unexpected. |
</threat_model>

<verification>
- `pytest tests/ -q` passes with `PENDING_QT_STRIP` empty.
- The probe returns `CLEAN` for `visoswap.processors` plus every discovered submodule in one run.
- No `video_processor` artefact exists under `visoswap/`.
- Comment-stripped token scan of `visoswap/` finds no Qt binding root, no Qt symbol, and no
  `app.ui` reference. The roadmap's own form of this check —
  `grep -ril "pyside\|qtcore\|qtwidgets" visoswap/processors/` — returns matches only in
  removal-note comments, and the tokenize-based test is the authoritative gate because it cannot be
  fooled by an inline comment on a code line.
- No file under `D:/Visomaster` was modified. Note that `D:/Visomaster/.git/config` is already
  malformed upstream, so `git -C D:/Visomaster ...` errors with `bad config line 1` — that is a
  pre-existing condition, not something this plan caused, and it is not a usable check. Verify
  instead with `find D:/Visomaster/app -type f -name '*.py' -newermt '-1 day'`, which must list
  nothing.
</verification>

<success_criteria>
1. Every module under `visoswap/` imports with `PySide6`, `PySide2`, `PyQt5`, `PyQt6`, `qtpy`,
   `shiboken6` and `shiboken2` all blocked, and none of them appears in `sys.modules` afterwards.
2. `visoswap/processors/` contains no `video_processor` module in any form.
3. `frame_worker.py` holds no host-window reference, no button state read, and neither former
   `app/ui` call site; the two former button states are booleans on `EngineContext`.
4. `PENDING_QT_STRIP` is empty and every vendored module is covered by both gates.
5. A human has confirmed the import in a virtualenv where `pip show PySide6` finds nothing, and
   `docs/verifying-qt-free.md` records how.
6. Nothing under `D:/Visomaster` was written to.
</success_criteria>

<phase_boundary>
State plainly in the SUMMARY, and do not soften it: Phase 1 proves that the vendored engine
**imports** without Qt. It does not prove the engine **runs**. The DFM, LivePortrait and CLIPseg
paths are vendored and import-clean, and nothing in this phase executed a single one of them. The
design names them as carrying possible coupling that static inspection did not find, and that risk
is fully intact at the end of Phase 1. Phase 2 exercises all three and is where it is retired.
</phase_boundary>

<output>
Create `.planning/phases/01-vendor-the-engine-strip-qt/01-04-SUMMARY.md` when done.
</output>
