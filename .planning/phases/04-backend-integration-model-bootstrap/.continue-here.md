---
context: phase
phase: 04-backend-integration-model-bootstrap
task: 3
total_tasks: 3
status: paused
last_updated: 2026-08-23T13:03:49.733Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Engine-heavy plans stall in subagent executors** — a gsd-executor spawned for plan 04-01 Task 3 ran ~22 minutes producing zero files/commits. The work requires real CUDA engine runs and a subagent apparently lost the thread. → **Execute engine-dependent plans (04-03, 04-04) directly in the orchestrator**, not via background subagents. Do not re-dispatch 04-01 as a subagent.

**Do not proceed until all boxes are checked.**

_If no constraints have been identified yet, remove this section._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Engine-heavy executor stall | A background executor for real-engine work produced nothing for 22 min | blocking | Execute 04-03/04-04 directly in the orchestrator; drive the real engine with bounded waits and the sealed runners (`_engine_runner.py`, `_backend_runner.py`) |
| Phantom complete plans | Plan 04-01 had 2 commits but no SUMMARY (interrupted session) | advisory | safe-resume gate: check SUMMARY.md exists before trusting plan state |

<current_state>
Phase 04 (Backend Integration & Model Bootstrap) is EXECUTING and now PAUSED. Plans 04-01 and 04-02 are complete and verified. Plans 04-03 and 04-04 remain. Full suite: 310 passed, 0 skips, on `.venv-clean/Scripts/python.exe` (the combined interpreter).
</current_state>

<completed_work>

Plans complete:
- Plan 04-01: Backend ported onto the engine path (24 files); ComfyUI removed structurally; provider default CUDA; EngineFrameGenerator behind the seam; source_face_path column; real tracer proven (CLEAN:tracer:frame=000000.000.jpg size=336835 provider=CUDA). Commits 152e05f, 306e3c2, aeb145e, 70af280, 3ddae24, 9e786fd.
- Plan 04-02: Recorder cancel-hang fixed (drain decoder stdout before wait). Cancelled-run test 0.28s. 14 recorder + 3 mechanism tests. docs/recorder-cancel-hang.md. Commits 8fc5d98, 24441de.

Also completed earlier this session: Phase 5 (frontend) planning — CONTEXT, UI-SPEC (approved), 5 PLAN.md files. UAT for Phases 1-3 (all passed, 18/18). A throwaway test dashboard (tools/dashboard.py, server closed).
</completed_work>

<remaining_work>

- Plan 04-03: Model bootstrap — manifest module, verify/repair, startup refusal on incomplete models (roadmap criteria 2 & 3). Plan file: .planning/phases/04-backend-integration-model-bootstrap/04-03-PLAN.md. Requires visoswap/models/manifest.py, bootstrap.py, tests, docs/model-bootstrap.md.
- Plan 04-04: Sever the VisoMaster install dependency; no-VisoMaster proof.
- Later: Phase 6 (End-to-End Verification). Phase 5 is planned but not executed.
</remaining_work>

<decisions_made>

- Combined runtime = `.venv-clean` (Py3.10); web stack installed there; Pillow pinned to engine's 9.5.0.
- ComfyUI removed structurally; backend column collapses to `engine`; provider default CUDA; TensorRT refused at settings-validation.
- `source_face_path` is a server-assigned project column, stripped from the public API.
- Recorder fix = drain decoder stdout before wait(), bounded by READ_TIMEOUT; the bounded-wait mitigation is rejected and recorded.
</decisions_made>

<blockers>
- None active. Two issues found during 04-01 (adapter missing settings; missing source_face_path) were fixed in commit 70af280. Executor-stall is a process constraint, not a code blocker.
</blockers>

## Required Reading (in order)

1. `.planning/phases/04-backend-integration-model-bootstrap/04-02-SUMMARY.md` — where Phase 4 execution left off (STATE.md resume file)
1. `.planning/phases/04-backend-integration-model-bootstrap/04-03-PLAN.md` — the next plan (model bootstrap)
1. `.planning/ROADMAP.md` — Phase 4 goal + criteria; Phase 5/6 status
1. `docs/backend-port.md` and `docs/recorder-cancel-hang.md` — the two records written this session

## Critical Anti-Patterns (do NOT repeat these)

- ENGINE-HEAVY SUBAGENT STALL: do not dispatch 04-03/04-04 to a background subagent expecting it to drive the real engine; run them directly with bounded waits. → structural mitigation: orchestrator-direct execution.
- Do not trust plan completion without a SUMMARY.md present (safe-resume gate).

## Infrastructure State

- Combined/engine interpreter: `.venv-clean/Scripts/python.exe` (Py3.10.19) — carries web + inference stacks. `pip show PySide6` reports absent. All backend + engine tests run here.
- Plain developer interpreter: `python` (3.13, Miniconda) — no torch/aiosqlite; runs non-backend tests.
- `model_assets/` is a junction into `D:/Visomaster` (read-only). 56 present files (12.23 GB), 6 TensorRT `.trt` files absent.
- No servers running (dashboard closed). No background jobs. No async job manifests.
- Uncommitted (NOT part of Phase 4): `tests/_engine_runner.py` (side-quest `--video` mode), `tools/dashboard.py` (side-quest). Leave them unless the user asks.

## Pre-Execution Critique Required

N/A — not pausing between design and execution.

<context>
The session did Phase 5 planning, then executed Phase 4 plans 04-01 and 04-02, fixing two real integration bugs (adapter settings, recorder hang). Execution approach that worked: orchestrator-direct with the sealed runners and bounded waits. The remaining 04-03/04-04 plans are substantial and engine/bootstrap-heavy; approach them the same way. User paused here deliberately.
</context>

<next_action>
Start with: `/gsd-execute-phase 04` to run plan 04-03 (model bootstrap), then 04-04 (sever VisoMaster). Execute directly in the orchestrator (not via background subagents). Full suite runs on `.venv-clean/Scripts/python.exe`.
</next_action>
