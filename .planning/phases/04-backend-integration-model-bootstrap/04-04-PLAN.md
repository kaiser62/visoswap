---
phase: 04-backend-integration-model-bootstrap
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-03"]
files_modified:
  - visoswap/models/models_data.py
  - visoswap/models/manifest.py
  - backend/config.py
  - backend/api/backends.py
  - backend/services/generator.py
  - backend/services/engine_backend.py
  - tests/conftest.py
  - tests/_engine_runner.py
  - tests/_backend_runner.py
  - tests/test_engine_seal.py
  - tests/test_no_visomaster_install.py
  - tests/test_vendor_headers.py
  - docs/engine-test-assets.md
  - docs/no-visomaster-install.md
  - .gitignore
autonomous: true
requirements: [ENGINE-01, BACKEND-01]

user_setup: []

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The models directory the engine loads weights from is read from the environment, and its default is a directory this project owns rather than a string pointing at a borrowed tree."
    - "The face-source picker lists and serves faces from a project-owned directory, so the one product feature that read straight out of a VisoMaster install no longer does."
    - "The seal is still proven non-inert against a package named `app` that genuinely resolves — against one this test builds, so the proof no longer requires a VisoMaster checkout to exist and holds on any machine."
    - "A swap runs to completion with a runtime guard armed that fails the run if any file opened anywhere in the call path resolves inside a VisoMaster install, and the guard is proven to have observed real opens rather than none."
    - "Nothing in this repository writes to, moves, deletes or renames anything under the read-only weight tree; the project-owned model set is a verified copy."
  artifacts:
    - visoswap/models/models_data.py
    - tests/test_no_visomaster_install.py
    - docs/no-visomaster-install.md
    - backend/config.py
  key_links:
    - "`visoswap/models/models_data.py:6` -> every `local_path` in both manifest lists: 62 f-strings interpolate `models_dir`, so changing that one name changes every tracked path at once. That is why it is the single edit site and why it must not be shadowed by a second resolution somewhere downstream."
    - "the runtime open guard -> `ENGINE-01` clause 2: a grep proves no *literal* names a VisoMaster path; only a guard watching real opens proves no *resolved* path reaches one. A junction resolves silently, and `os.path.islink('model_assets')` returns False for it on this machine — measured — so a static check on link-ness would have reported clean while the swap read 12 GB out of the borrowed tree."
    - "`tests/test_engine_seal.py` -> a self-built `app` package: today the non-inertness proof depends on the very install `ENGINE-01` says must not be needed, so closing the requirement would break the test that guards the seal. Building the stand-in is what lets both be true at once, and it makes the proof stronger rather than weaker because it then holds on every machine."
    - "`tests/_engine_runner.py`'s media defaults -> plan 02-02's non-zero pixel diff: the smoke video and source face are personal media that live in `D:/Visomaster/output` and `D:/Visomaster/inputt` by default. They cannot be committed (T-02-10) and they cannot keep those defaults, so the fixture location moves and the missing-asset exit stays the answer when it is unset."
---

<!-- planner-discipline-allow: comfyui, PySide6, visomaster, visomaster_dir, VISOMASTER_DIR, DEFAULT_VISOMASTER_DIR, DEFAULT_ENGINE_PYTHON, VISOSWAP_ENGINE_PYTHON, VISOSWAP_TEST_VIDEO, VISOSWAP_TEST_SOURCE, DEFAULT_TEST_VIDEO, DEFAULT_TEST_SOURCE, models_dir, MODELS_DIR, model_assets, inputt, inswapper_128.fp16.onnx, liveportrait_onnx, app.ui.widgets.common_layout_data, islink, realpath, junction, mklink, rmdir, viso_dir, _local_sources -->

<objective>
Sever every dependency this project still has on a VisoMaster install being present, and prove the
severance at runtime rather than by grep — closing `ENGINE-01`, which has been `Pending` since
Phase 2 with its first clause proven and its second false.

Purpose: `ENGINE-01` reads "swap a frame with no PySide6 installed **and no VisoMaster install
present**". Plan 02-04 measured clause 2 as false and named this phase as the one that can close it,
because until the weights are the project's own, "no VisoMaster install present" is a claim this
repository cannot make. It closes here and nowhere else.

Output: an env-driven models directory, a project-owned face-source directory, test fixtures that no
longer default into a borrowed tree, a seal proof that builds its own subject, and a runtime guard
that fails a swap if any opened file resolves inside a VisoMaster install.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/specs/2026-08-21-visoswap-standalone-design.md
@.planning/phases/02-engine-api-first-swap/02-04-SUMMARY.md
@.planning/phases/04-backend-integration-model-bootstrap/04-01-SUMMARY.md
@.planning/phases/04-backend-integration-model-bootstrap/04-03-SUMMARY.md
@docs/engine-test-assets.md
@tests/conftest.py
@tests/test_engine_seal.py
</context>

<source_boundary>
`D:/Visomaster` is **read-only source material** and this plan is the one most exposed to violating
that. Nothing here may write, move, delete or rename anything under it — including the
`model_assets` junction's target. The project-owned model set is produced by **copying**, never by
moving, and never by "relocating" the junction. Never run `git -C D:/Visomaster ...`.
`C:/Users/Sakat/.devswarm/repos/1/ef7f9c3d/config-parallel-setup` is **read-only reference
material**. Scratch work goes only in
`D:/Dev/tmp/claude/C--Users-Sakat--devswarm-repos-1-ef7f9c3d-config-parallel-setup/a72e48bc-b4c0-47a4-b3ab-f98701283bde/scratchpad/`.
</source_boundary>

<measured_facts>
Measured during planning. Plan against these; do not re-derive them.

**`ENGINE-01` clause 2 is broken by five things, not the three named in the phase brief.** Each was
located and read:

  1. **The weights.** `visoswap/models/models_data.py:6` is `models_dir = './model_assets'`, and
     `model_assets` is a junction whose real path is `D:\VisoMaster\model_assets`. All 56 required
     model files, 12.23 GB, physically live inside the VisoMaster install — including the 277 MB
     `inswapper_128.fp16.onnx` that every swap loads.
  2. **The engine interpreter default.** `tests/conftest.py:72` is
     `DEFAULT_ENGINE_PYTHON = Path("D:/Visomaster/dependencies/Python/python.exe")`, used whenever
     `VISOSWAP_ENGINE_PYTHON` is unset.
  3. **The seal's non-inertness proof.** `tests/test_engine_seal.py:69`
     `test_the_seal_is_not_inert_against_visomaster` asserts `app=yes` — that VisoMaster's `app`
     package genuinely resolved before the seal armed — and its own failure message says "Point
     `VISOMASTER_DIR` at a real checkout." The `app` package it needs comes from
     `tests/_engine_runner.py:75` `DEFAULT_VISOMASTER_DIR = D:/Visomaster`, appended to `sys.path` at
     four sites (lines 304, 401, 488, 775).
  4. **The test media.** `tests/_engine_runner.py:100-105`:
     `DEFAULT_TEST_VIDEO = D:/Visomaster/output/17f0d620_rosh_generate_135bda4c9686.mp4` and
     `DEFAULT_TEST_SOURCE = D:/Visomaster/inputt/598004cb_tonima.JPG`. Both are overridable
     (`VISOSWAP_TEST_VIDEO`, `VISOSWAP_TEST_SOURCE`) and both are personal media the runner's own
     comment says are deliberately not committed. The smoke swap, the LivePortrait run and plan
     04-01's tracer all resolve them.
  5. **The face-source picker, which is on the product path and not the test path.** This one is not
     in the phase brief and it is the most important of the five, because the other four are
     developer-machine defaults while this is a shipped feature. `backend/config.py:38` declares
     `visomaster_dir: Path | None`. `backend/api/backends.py:145` `_local_sources` lists
     `Path(viso_dir) / "inputt"` and `:167` `visomaster_source_thumbnail` serves image bytes out of
     the same folder, with `:185-187` raising 404 when the setting is unset.
     `backend/services/generator.py:315` and `:341` pass `viso_dir` into the in-process backend and
     `generator.py:328-343`'s `source_path` resolves `self.viso_dir / "inputt" / name`. A user
     picking a face is reading the VisoMaster install, live.

**`os.path.islink('model_assets')` returns `False` on this machine while `os.path.realpath` returns
`D:\VisoMaster\model_assets`.** It is a Windows junction created by `mklink /J`, not a symlink. Any
static severance check written against link-ness would report clean while every swap read out of the
borrowed tree. Only a check on the *resolved* path sees it.

**The file is already not byte-identical to upstream, and the brief's framing needs one correction.**
`diff --strip-trailing-cr` between the vendored body from line 5 and
`D:/Visomaster/app/processors/models_data.py` reports exactly one hunk: the vendored line 6 carries a
trailing comment — "Vendored verbatim. Phase 4's model bootstrap makes this env-driven (MODELS_DIR);
until then it must diff clean against upstream" — that upstream does not have. So Phase 1 already
opened the named edit site and wrote the provenance note; what remains is to change the *code* at a
site that is already marked, not to break an identity that is already broken. The line's executable
text is still character-for-character upstream's, and that is what the change ends.

**Disk: `D:` has 68 GB free of 367 GB. `C:` has 6.2 GB free.** A project-owned copy of the 12.23 GB
model set fits on `D:` and does not fit on `C:`. The copy destination must be on `D:`, and free space
must be checked before the copy rather than discovered partway through it.

**The complete set is hash-correct where it stands.** Plan 04-03 measured all 56 required files
present, totalling 12,233,143,713 bytes, and `inswapper_128.fp16.onnx` hashing to its manifest digest
exactly. So a copy can be verified against the same manifest that verified the original, and a
partial copy is detectable rather than assumed complete.

**A full hash pass over the set costs about 7 seconds warm** (1,786 MB/s measured), so verifying the
copy is cheap relative to making it.

**Clause 1 of `ENGINE-01` is already proven and must stay proven.** Plan 02-04 measured
`.venv-clean/Scripts/python.exe` as CPython 3.10.19 with `PySide6`, `PySide2`, `PyQt5`, `PyQt6`,
`qtpy` and `shiboken6` all `ABSENT` and `app` resolving to `None`, and ran the full suite green
against it. Whatever this plan changes, that measurement must be re-taken and re-stated, not
inherited.

**The seal blocks nine roots and reports reachability before arming.** `tests/_blocked_roots.py`
holds the single definition; the runner measures `find_spec` for each root *before* installing the
`sys.meta_path` finder, because a finder at index 0 raises on the name and would report identical
success whether or not the package exists. The measured line on the Qt interpreter is
`PyQt5=no,PyQt6=no,PySide2=no,PySide6=yes,app=yes,backend=no,qtpy=yes,shiboken2=no,shiboken6=yes`.
`app=yes` is the assertion at stake here.
</measured_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The models directory becomes the project's own, by copy and by environment</name>
  <precondition>Plan 04-03 has landed `visoswap/models/manifest.py` and `visoswap/models/bootstrap.py`, so the copy can be verified against the same manifest that verifies the original. At least 13 GB is free on the destination volume — 68 GB measured free on `D:` at planning time, and the check is the task's own, not an assumption.</precondition>
  <files>visoswap/models/models_data.py, visoswap/models/manifest.py, backend/config.py, tests/test_no_visomaster_install.py, tests/test_vendor_headers.py, .gitignore</files>
  <behavior>
    - The vendored models directory value comes from `MODELS_DIR` when set, and from a project-owned default when it is not.
    - The default is a path inside this repository and resolves to no location inside a VisoMaster install.
    - Every one of the 62 tracked paths follows the change, because they all interpolate the same name.
    - Setting `MODELS_DIR` to a directory relocates every tracked path under it, and clearing it returns them to the default.
    - The copy of the model set verifies against the same manifest digests as the original, entry by entry.
    - A copy that is interrupted is detected as incomplete rather than reported as done.
  </behavior>
  <action>
Change `visoswap/models/models_data.py:6` — the named edit site Phase 1 already marked — so the value
comes from the environment with a project-owned default. Follow the discipline the `clipseg.py` and
`cliplib/clip.py` changes established: the smallest possible divergence, with a trailing provenance
comment saying what changed, why, and that it is not upstream text. One line of executable change.
Do not restructure the file, do not reformat the lists, do not touch the 62 f-strings — they all
interpolate `models_dir` and they all follow it for free, which is the entire reason this is one edit
site and not sixty-two.

Resolve it the way `visoswap/schema/__init__.py` already resolves the same idea: `MODELS_DIR` first,
then a repository-relative default. Do not invent a third order. If `visoswap/models/manifest.py`
from plan 04-03 already owns that resolution, the vendored file should read through it rather than
reimplement it — but keep the vendored file's own import surface minimal, since it is a data module
that plan 04-03's manifest imports and a cycle between the two would be a worse outcome than a
duplicated three-line resolver. Decide, and say which you chose and why in the module comment.

Update `tests/test_vendor_headers.py` and `docs/engine-test-assets.md`: the file is still vendored and
still carries its attribution header, but it now has two named edit sites rather than one, and the
docs should list them the way the two `torch.load` sites are listed.

**Make the copy.** Write it as a mode on the existing `tools/link_model_assets.py` or as a sibling
tool — that file already knows how to refuse dangerous destinations and already has four exercised
failure paths, and its first threat was exactly "overwriting weights is the one unrecoverable
mistake here". Whichever you choose, these refusals are not optional:

  - Refuse any destination whose resolved real path lies inside a VisoMaster install.
  - Refuse to copy *from* a source that is not readable, and refuse to proceed if the destination
    volume has less free space than the manifest's total size plus a margin.
  - Copy, never move; never `rmdir`, `rm -rf` or rename anything under the source. On Windows a
    recursive delete aimed at a junction can follow it into the target, and the target here is 12.23
    GB of weights this project must not touch.
  - Verify every copied entry against its manifest digest afterwards and report the three states plan
    04-03 defined. A copy is complete when the verifier says so, not when the loop ends.

Do **not** remove the `model_assets` junction. It costs nothing to leave, it is not what makes clause
2 false — the *default pointing at it* is — and removing it is the one action in this phase that
cannot be undone by hand. Say so in `docs/no-visomaster-install.md`.

Add the project-owned model directory to `.gitignore`. Add a `MODELS_DIR` setting to
`backend/config.py` if plan 04-03 did not, or reconcile with it if it did; there must be exactly one.

Write the model-directory half of `tests/test_no_visomaster_install.py`: assert the default resolves
outside any VisoMaster install; assert setting `MODELS_DIR` moves every tracked path under it and
clearing it moves them back; and assert against the *resolved* path in both cases, not the literal —
`os.path.islink` returns `False` for the junction on this machine, measured, so a link check proves
nothing.
  </action>
  <verify>
    <automated>cd D:/Dev/visoswap &amp;&amp; python -m pytest tests/test_no_visomaster_install.py -x -q</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; python -c "
import os, pathlib
os.environ.pop('MODELS_DIR', None)
import importlib, visoswap.models.models_data as md
importlib.reload(md)
bad = [e['local_path'] for e in md.models_list if 'visomaster' in str(pathlib.Path(e['local_path']).resolve()).lower()]
assert not bad, 'default still resolves inside a VisoMaster install: %r' % bad[:3]
print('default models directory resolves outside any VisoMaster install')
"</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; diff --strip-trailing-cr &lt;(sed -n '5,$p' visoswap/models/models_data.py) "D:/Visomaster/app/processors/models_data.py" | grep -c "^[&lt;&gt;]" ; echo "expected: a small, countable divergence confined to the named edit sites"</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; python -m pytest tests/test_vendor_headers.py tests/test_model_manifest.py tests/test_model_bootstrap.py -q</automated>
  </verify>
  <done>
`models_dir` reads `MODELS_DIR` with a project-owned default, changed at one named edit site with a
provenance comment, and the diff against upstream is still confined to named edit sites. A verified
copy of the required model set exists outside any VisoMaster install, every entry matching its
manifest digest. The junction is untouched. `MODELS_DIR` has exactly one definition across the
backend and the manifest.
  </done>
  <reversibility rating="costly">The copy consumes 12.23 GB on `D:`, which had 68 GB free. Reversible by deleting the copy and unsetting `MODELS_DIR`, at no risk to anything — the source is never written, moved or removed, and the junction stays in place. The cost is disk and time, not risk, which is why it is not a checkpoint.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The face sources, the test defaults, and a seal that builds its own subject</name>
  <precondition>Task 1 is complete, and plan 04-01 has landed `backend/api/backends.py` and `backend/services/generator.py` in this repository.</precondition>
  <files>backend/config.py, backend/api/backends.py, backend/services/generator.py, backend/services/engine_backend.py, tests/conftest.py, tests/_engine_runner.py, tests/_backend_runner.py, tests/test_engine_seal.py, tests/test_no_visomaster_install.py, docs/engine-test-assets.md</files>
  <behavior>
    - The face-source listing and the face-source thumbnail both read a project-owned directory whose location comes from configuration, and neither mentions a VisoMaster folder name.
    - A source name is still rejected when it is not a bare filename, and the resolved path is still asserted to sit directly inside the configured folder.
    - The engine interpreter default is a project-owned interpreter, and pointing the override at a Qt-carrying one still works.
    - The smoke media resolve from a project-owned directory by default, and an absent one is reported as a missing asset rather than skipped.
    - The seal is still proven non-inert against a package named `app` that genuinely resolves before the seal arms, without any VisoMaster checkout being present.
    - With no VisoMaster checkout reachable at all, the whole suite still passes and nothing is skipped.
  </behavior>
  <action>
**The product path first, because it is the one a user touches.** Rename `visomaster_dir` in
`backend/config.py` to name what it now is — a directory of source face images this project owns —
and stop appending `inputt` to it. That folder name is VisoMaster's, and keeping it means the setting
still describes someone else's layout. Update `_local_sources`, `visomaster_source_thumbnail` and
`generator.source_path` together; they are three readers of one folder and splitting them is how one
gets missed.

Keep every validation exactly as it is. The name must equal its own basename, must not be `.` or
`..`, must carry an image suffix, and the resolved path's parent must equal the resolved folder. That
chain is the mitigation for T-04-01 and none of it is being changed here — only which folder it
resolves inside. Say that in the summary, because a reviewer seeing this diff will reasonably ask
whether a path check moved.

The route paths themselves are frontend-visible and Phase 5 renders against them. Decide whether to
rename them and record the decision; if they are renamed, note it as a breaking change for Phase 5 in
the summary rather than leaving Phase 5 to discover a 404.

**The test defaults next.** `tests/conftest.py:72`'s `DEFAULT_ENGINE_PYTHON` becomes the project's own
interpreter — `.venv-clean`, or whichever combined interpreter plan 04-01 settled on. Two properties
have to survive that change and they pull in opposite directions, so handle both explicitly:

  - Plan 02-04 recorded that the *old* default proved "the engine never reaches Qt" (a seal property,
    because that interpreter has PySide6 6.7.2) while `.venv-clean` proves "the engine runs where Qt
    is genuinely absent" (clause 1). Flipping the default makes the strong claim the default one,
    which is right — but the seal property must remain provable by pointing the override at a
    Qt-carrying interpreter, and `docs/engine-test-assets.md` must say so with the exact command.
  - Nothing may skip when the Qt-carrying interpreter is absent. Whatever asserts the seal property
    must either construct its own subject (below) or fail.

`tests/_engine_runner.py`'s `DEFAULT_TEST_VIDEO` and `DEFAULT_TEST_SOURCE` move to a project-owned,
gitignored media directory. They stay uncommitted — they are personal media and committing one is an
information-disclosure bug, which is T-02-10 and unchanged. The env overrides stay. A missing file
stays `ASSET_MISSING (3)` naming the resolved path, never a skip. Populate the directory by copying
from wherever the developer's copies live; never move them, and never read them from the source tree
at run time.

**The seal, which is the interesting one.** `test_the_seal_is_not_inert_against_visomaster` currently
proves the seal works by refusing a package that really resolves — and the only such package is
VisoMaster's own `app`, from the install this requirement says must not be needed. Deleting the
assertion would close `ENGINE-01` by making the seal proof vacuous, which is the exact failure plan
01-04 caught and plan 02-01 built the whole reachability report to avoid.

Build the subject instead. Create a minimal package named `app` in a temporary directory, with enough
of a module path to stand in for the blocked one, put that directory on `sys.path` **before** the
seal arms, and assert the reachability line reports it resolving and that importing it afterwards
raises. That is a strictly stronger proof than today's: it holds on any machine, it does not depend on
an install this project is severing, and it can be extended to the `backend` root, which plan 02-01
had to report honestly as `backend=no` because the package did not exist yet and which now does.

Keep `VISOMASTER_DIR` support in the runner as an override — pointing it at a real checkout should
still work and should still be the sharper run — but the *default* must no longer be a path inside
`D:/Visomaster`, and the test must not need it.
  </action>
  <verify>
    <automated>cd D:/Dev/visoswap &amp;&amp; python -m pytest tests/test_engine_seal.py -x -q</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; python -m pytest tests/test_no_visomaster_install.py -x -q</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; VISOMASTER_DIR="/nonexistent-checkout" python -m pytest tests/ -q -rs</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; grep -rn "inputt" backend/ visoswap/ --include="*.py" | grep -v __pycache__ ; test -z "$(grep -rn 'inputt' backend/ visoswap/ --include='*.py' | grep -v __pycache__)" &amp;&amp; echo "no VisoMaster source folder name on the product path"</automated>
  </verify>
  <done>
The face-source listing and thumbnail read a project-owned configured directory with every path
validation intact and no VisoMaster folder name anywhere on the product path. The engine interpreter
and smoke media defaults are project-owned, with the Qt-carrying run still available by override and
documented. `test_the_seal_is_not_inert_against_visomaster` proves non-inertness against a package it
builds itself, and the whole suite passes with `VISOMASTER_DIR` pointed at a directory that does not
exist, with zero skips.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove clause 2 at runtime, and close ENGINE-01</name>
  <precondition>Tasks 1 and 2 are complete, the project-owned model set verifies against the manifest, and plan 04-01's `tests/_backend_runner.py --tracer` runs green.</precondition>
  <files>tests/_backend_runner.py, tests/_engine_runner.py, tests/test_no_visomaster_install.py, docs/no-visomaster-install.md</files>
  <action>
A grep proves no *literal* names a VisoMaster path. Only a guard watching real opens proves no
*resolved* path reaches one — and on this machine that distinction is not theoretical: the junction
resolves silently and `os.path.islink` reports `False` for it.

Add a mode to the runners that installs an open guard for the duration of a real run: wrap the
builtin file open and the low-level `os.open` so every path they receive is resolved and compared
against a configured set of forbidden roots, and **raise** — not warn, not log — when one lands
inside a VisoMaster install. Run the full path under it: load a video, detect a face, swap a frame,
and in the backend runner drive it through the generation route, exactly as plan 04-01's tracer does.

Non-vacuity is the whole game here and it has two halves, both mandatory:

  - The guard must **report how many opens it observed**, and the test must assert that count is
    large. A guard that saw zero opens passes perfectly and proves nothing, and a swap that loads a
    277 MB ONNX model opens a great many files. This is the same rule plan 02-01 established for the
    seal and plan 03-03 for the reassignment scanner, applied to a third gate.
  - The guard must be shown **capable of firing**. Point it at a forbidden root that the run really
    does touch — a temporary directory the test itself opens a file inside, added to the forbidden
    set — and assert the run fails. Without that, a guard with a broken comparison is
    indistinguishable from a clean run.

Then take the two measurements `ENGINE-01` actually asks for and state them together, because the
requirement is a conjunction and half of it has been true since Phase 2:

  - **Clause 1, re-measured not inherited.** On the Qt-free interpreter, report `PySide6`, `PySide2`,
    `PyQt5`, `PyQt6`, `qtpy` and `shiboken6` as absent, `pip show PySide6` reporting not found, and
    the swap completing. Plan 02-04 recorded the trap: with the override unset the old default
    resolved to a PySide6-carrying interpreter, so a green run proved a seal property rather than
    clause 1.
  - **Clause 2, newly true.** The same swap completing with `MODELS_DIR` at the project-owned copy and
    the open guard armed against every VisoMaster root, having observed a large number of opens and
    having been shown capable of firing.

Add the static half too, since it is cheap and catches a literal a runtime guard would only catch on
a path that happened to execute: scan `visoswap/` and `backend/` for any string literal naming a
VisoMaster install path, strip comments with `tokenize` the way `tests/test_no_qt_source.py` does so
a removal note stays legal, report the examined-file count and assert it is non-zero. Exclude
`tests/`, which legitimately names the checkout in overrides and in the seal's own documentation, and
say why in the module docstring — an unexplained exclusion in a gate is indistinguishable from a hole.

Write `docs/no-visomaster-install.md`: the five dependencies as found, what each became, the junction
left deliberately in place and why, the `islink`-versus-`realpath` trap with the measured result, the
open guard and both halves of its non-vacuity proof, the two clause measurements verbatim, and the
one thing that is still true — that a developer *may* still point `MODELS_DIR`, `VISOMASTER_DIR`,
`VISOSWAP_ENGINE_PYTHON` and the media overrides at a VisoMaster checkout, because the requirement is
that the project does not *need* one, not that it refuses to use one.

Do not edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md`. State in the
summary that `ENGINE-01` is now satisfied in both clauses, with the evidence, and leave the marking
to the owner — the same convention plans 02-04 and 03-02 followed.
  </action>
  <verify>
    <automated>cd D:/Dev/visoswap &amp;&amp; python -m pytest tests/test_no_visomaster_install.py -q</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; "$VISOSWAP_APP_PYTHON" -B tests/_engine_runner.py --no-visomaster</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; "$VISOSWAP_APP_PYTHON" -B tests/_backend_runner.py --tracer --no-visomaster</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; "$VISOSWAP_APP_PYTHON" -m pip show PySide6 2&gt;&amp;1 | grep -q "not found" &amp;&amp; echo "clause 1: PySide6 absent"</automated>
    <automated>cd D:/Dev/visoswap &amp;&amp; test -s docs/no-visomaster-install.md &amp;&amp; python -m pytest tests/ -q -rs</automated>
  </verify>
  <done>
A swap and a full generation request both complete with the open guard armed against every VisoMaster
root, with the guard reporting a large observed-open count and having been shown capable of firing
against a planted forbidden root. `PySide6` is measured absent on the interpreter that ran them. The
static literal scan reports a non-zero examined-file count and finds nothing outside comments.
`docs/no-visomaster-install.md` records all five severances, the `islink` trap and both clause
measurements. `ENGINE-01` is satisfied in both clauses, stated with evidence and left for the owner to
mark.
  </done>
  <reversibility rating="reversible">Every severance is a default change plus an environment variable, and every old target remains reachable by setting one. Nothing was deleted, moved or renamed under the read-only tree, so the previous configuration is one export away.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → `D:/Visomaster` | A read-only tree holding 12.23 GB of irreplaceable weights and personal media is read, copied from, and never written. |
| `MODELS_DIR` → the copier and the engine | An environment value decides which directory is read, hashed, and written into. |
| a source-face name → the configured faces folder | A user-supplied name becomes a filesystem path and a byte stream served over HTTP. |
| the open guard → `ENGINE-01` | A runtime interceptor decides whether a whole requirement is satisfied. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-21 | Tampering | A recursive delete or rename aimed at the `model_assets` junction following it into the weight tree | critical | mitigate | The junction is deliberately left in place and no task removes, renames or moves it. The copier refuses any destination resolving inside a VisoMaster install and copies rather than moves. `os.path.islink` returning `False` for a junction is recorded as measured, so no check may rely on it. This is the one unrecoverable mistake available in the phase. |
| T-04-22 | Tampering | A partial copy of the model set presenting as complete | high | mitigate | The copy is verified entry by entry against the same manifest digests plan 04-03 defined, reporting present-and-matching, present-and-mismatching and absent separately, and free space is checked before the copy rather than discovered during it. |
| T-04-23 | Tampering | Path traversal through a source-face name against the new faces folder | high | mitigate | The existing validation chain moves unchanged: the name must equal its own basename, must not be `.` or `..`, must carry an image suffix, and the resolved path's parent must equal the resolved folder. Only the folder changes; the chain does not, and the summary is required to say so. |
| T-04-24 | Spoofing | A vacuous open guard reporting clause 2 satisfied | high | mitigate | The guard reports its observed-open count and the test asserts it is large; the guard is separately shown capable of firing against a planted forbidden root that the run really touches. Both halves are required, because a guard that saw nothing and a guard that compares wrongly both produce a clean run. |
| T-04-25 | Information disclosure | Personal media or a derivative of it entering the repository | medium | mitigate | The smoke media stay uncommitted and their new home is gitignored, as T-02-10 already required; the media directory is populated by copy and never read from the source tree at run time. |
| T-04-26 | Repudiation | Closing `ENGINE-01` by weakening the seal's non-inertness proof | high | mitigate | The proof is not removed; its subject is rebuilt. The test constructs an `app` package that genuinely resolves before the seal arms, so the assertion holds on machines with no VisoMaster checkout and the proof is strictly stronger than the one it replaces. |
| T-04-27 | Elevation of privilege | The faces folder defaulting to a directory outside the project | medium | mitigate | The setting's default is inside the project's own data directory, and an unset value produces the same 404 the reference produced rather than a silent fallback to a wider directory. |
| T-04-SC | Tampering | pip/npm/cargo installs | high | mitigate | This plan installs nothing. It uses `os`, `pathlib`, `shutil`, `ast`, `tokenize` and `hashlib` from the standard library. If an install becomes necessary, halt and require the package-legitimacy gate plus a blocking human checkpoint. |
</threat_model>

<verification>
1. `python -m pytest tests/ -q -rs` passes with zero skips, both with `VISOMASTER_DIR` unset and with
   it pointed at a directory that does not exist.
2. With `MODELS_DIR` unset, every tracked model path resolves outside any VisoMaster install.
3. `tests/_engine_runner.py --no-visomaster` and `tests/_backend_runner.py --tracer --no-visomaster`
   both exit 0, both report a large observed-open count, and the guard is separately shown firing.
4. `pip show PySide6` against the interpreter that ran them reports the package not found, and the
   Qt-binding absence census is re-measured rather than quoted from plan 02-04.
5. The static literal scan over `visoswap/` and `backend/` reports a non-zero examined-file count and
   finds no VisoMaster install path outside comments.
6. `visoswap/models/models_data.py` still carries its attribution header, and its diff against
   upstream is confined to the two named edit sites.
7. **`D:/Visomaster` is unmodified.** Nothing under it outside `dependencies/` is newer than this
   plan's first commit; `model_assets` still resolves to the same target; the 56 required files still
   number 56 and still total 12,233,143,713 bytes. Do not run any git command against
   `D:/Visomaster`.
8. `git -C "C:/Users/Sakat/.devswarm/repos/1/ef7f9c3d/config-parallel-setup" status --short` prints
   nothing.
</verification>

<success_criteria>
- `ENGINE-01` is satisfied in **both** clauses: a frame swaps with no PySide6 installed, measured
  fresh, and with no VisoMaster install needed anywhere in the call path, proven by a runtime open
  guard that is itself proven non-vacuous in two directions.
- All five dependencies on a VisoMaster install are severed — the weights, the engine interpreter
  default, the seal's proof subject, the test media defaults, and the face-source picker on the
  product path — and the fifth, which the phase brief did not name, is recorded as found.
- The seal's non-inertness proof is stronger than it was, not weaker: it holds on any machine.
- Nothing under the read-only weight tree was written, moved, renamed or deleted.
</success_criteria>

<output>
Create `.planning/phases/04-backend-integration-model-bootstrap/04-04-SUMMARY.md` when done. Lead
with the fifth dependency — the face-source picker reading `<visomaster_dir>/inputt` on the product
path — because the phase brief named three and the roadmap named none, and a summary that reports
"the three severances are done" against five would leave a shipped feature reading someone else's
install. State `ENGINE-01`'s two clause measurements together and leave the requirement marking to
the owner.
</output>
