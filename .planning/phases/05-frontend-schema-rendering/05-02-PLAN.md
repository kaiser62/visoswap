---
phase: 05-frontend-schema-rendering
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - backend/api/settings.py
  - backend/api/schemas.py
  - tests/test_api_settings_write.py
autonomous: true
requirements:
  - FRONTEND-01
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PUT /api/projects/{id}/settings persists exactly the provided overrides to the project tier and a later GET (fresh connection) returns them — the round-trip the FRONTEND-01 reload criterion depends on"
    - "PUT rejects an unknown key, a wrong-tier key, and a value outside the schema's contract with 400 and stores nothing (no partial writes)"
    - "POST /api/projects/{id}/presets/{preset} applies the seeded preset atomically, returns a report plus updated values, and answers 404 for an unknown preset"
  artifacts:
    - path: "backend/api/schemas.py"
      provides: "pydantic request/response models for the write endpoints (SettingsUpdate, PresetApplyResponse)"
      exports: ["SettingsUpdate"]
    - path: "tests/test_api_settings_write.py"
      provides: "write-API contract tests (PUT round-trip, rejection matrix, preset apply)"
      contains: "test_"
  key_links:
    - from: "backend/api/settings.py"
      to: "visoswap/settings/store.py"
      via: "store.set_project(conn, project_id, key, value) per provided key, then commit — validates and JSON-encodes"
      pattern: "set_project"
    - from: "backend/api/settings.py"
      to: "visoswap/settings/presets.py"
      via: "presets.apply_preset(conn, preset_id, project_id) inside a caller-owned transaction (commit/rollback in the handler)"
      pattern: "apply_preset"
---

<objective>
Write half of D-05: PUT /api/projects/{project_id}/settings (persist only the changed values a user actually touched, per D-02) and POST /api/projects/{project_id}/presets/{preset_id} (apply a migrated preset, per D-04), wrapping the Phase 3 store on the sync-connection bridge proven in plan 01.

Purpose: The frontend save and preset-apply flows (plans 04-05) need a real write path with the Phase 3 validation semantics — reject at the boundary, store nothing partial, and return the freshly resolved values so the UI can reconcile.

Output: Two write endpoints with pydantic request models in backend/api/schemas.py, a shared error-mapping helper reused from plan 01, and a contract test file proving persistence round-trip, the rejection matrix, and atomic preset application.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-schema-rendering/05-CONTEXT.md
@.planning/phases/05-frontend-schema-rendering/05-UI-SPEC.md
@visoswap/settings/store.py
@visoswap/settings/presets.py
@backend/api/settings.py
@backend/api/schemas.py

<interfaces>
Built in plan 01 and reused here (read .planning/phases/05-frontend-schema-rendering/05-01-SUMMARY.md only if a signature is unclear; the sources are authoritative):

From backend/api/settings.py (plan 01):
- router = APIRouter(tags=["settings"]) with GET /api/schema, GET /api/projects/{project_id}/settings, GET /api/presets
- the sync-bridge context manager (connects sqlite3 to get_settings().db_path, PRAGMA foreign_keys=ON, closes)
- the readiness helper (apply_settings_schema + seed_presets once per db path)
- the shared error-mapping helper: store.UnknownSettingsKey / store.InvalidSettingValue / store.WrongTier -> HTTPException 400 with detail=str(exc)

From visoswap/settings/store.py (write semantics — caller owns the transaction, nothing commits):
```python
def set_project(connection, project_id, key, value, models_dir=None) -> None
    # _check_key(key, "project") raises WrongTier for a global-tier key;
    # validate() raises InvalidSettingValue for wrong type / out of bounds / option not listed;
    # unknown key raises UnknownSettingsKey; value is JSON-encoded; upsert into project_settings
```

From visoswap/settings/presets.py:
```python
def get_preset(connection, preset_id) -> dict          # raises presets.UnknownPreset(KeyError)
def apply_preset(connection, preset_id, project_id, models_dir=None) -> dict
    # -> {"project_written","project_cleared","global_written","global_cleared","unavailable"}
    # writes overrides only, clears overrides that agree with the default, skips keys whose value
    # is not an available option (DFM models) and reports them in "unavailable"
    # DOES NOT COMMIT — the caller owns the transaction
```

The API contract additions this plan creates (consumed by plans 04-05):
- PUT /api/projects/{project_id}/settings  body {"overrides": {key: value}}  -> 200 {"values": {...}}; 400 {detail} on invalid; 404 unknown project; empty overrides -> 200 no-op
- POST /api/projects/{project_id}/presets/{preset_id}  -> 200 {"report": {...}, "values": {...}}; 404 unknown preset or project
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Tracer: PUT settings round-trip through FastAPI -> store -> SQLite, changed values only</name>
  <precondition>The plan 01 read API is merged: `python -c "from backend.api.settings import router"` imports cleanly and tests/test_api_settings.py passes. Halt if not.</precondition>
  <reversibility rating="costly">D-05 extends the public API surface with a mutating endpoint; the request/response shape becomes a contract later callers depend on.</reversibility>
  <files>backend/api/settings.py, backend/api/schemas.py, tests/test_api_settings_write.py</files>
  <action>
    Add the write endpoint to backend/api/settings.py, reusing the plan 01 bridge, readiness helper and error mapper. Per D-05 and D-02 (the backend persists only the overrides a caller sends — matching the frontend's dirty-only save):
    1. In backend/api/schemas.py, add pydantic models following the existing style: SettingsUpdate with a single field overrides: dict[str, bool | int | float | str] (arbitrary keys — per-key validation is the store's job, not the schema's, so an unknown key or bad value reaches store.set_project and is rejected there with the correct error type), and a SettingsResponse model {"values": dict[str, bool | int | float | str]} for the 200 body.
    2. PUT /api/projects/{project_id}/settings handler: depend on get_project (404/400 on the id), parse SettingsUpdate, then run one synchronous block in asyncio.to_thread: connect, ensure ready, iterate body.overrides.items() calling store.set_project(conn, project_id, key, value) for each, commit, read store.resolve_all(conn, project_id=project_id), close. Return {"values": resolved}. Empty overrides: return the current values without writing anything.
    3. Rejection semantics: on the FIRST store error (UnknownSettingsKey, InvalidSettingValue, WrongTier) roll back and answer 400 with the message — nothing may be partially persisted, and a body containing both a valid and an invalid key must store neither. Map sqlite3.Error to 500 and roll back.
    4. The API must not coerce types: a JSON string "1" for an int key, or a bool for an int key, is a 400 from the store, never a silent conversion.

    Write tests/test_api_settings_write.py on the same TestClient + temp-database fixture pattern as tests/test_api_settings.py (create the project via POST /api/projects). Cover: PUT two project-tier keys -> 200 with values reflecting them; a fresh GET (new request, same fixture database) returns the changed values — the persistence round-trip; PUT of a global-tier key (e.g. AudioMuxingToggle) into the project endpoint -> 400 (WrongTier) and nothing stored; PUT of an unknown key -> 400; PUT of an out-of-bounds int -> 400; PUT of a string where the entry wants an int -> 400; PUT {} -> 200 no-op; PUT to an unknown project -> 404; a mixed body (one valid + one invalid key) -> 400 and the valid key is NOT stored.
  </action>
  <verify>
    <automated>.venv-clean/Scripts/python.exe -m pytest tests/test_api_settings_write.py -x</automated>
  </verify>
  <done>PUT persists exactly the provided overrides to the project tier, round-trips through a fresh connection, rejects the full invalid-input matrix with 400 and zero partial writes, and answers 404 for an unknown project.</done>
</task>

<task type="auto">
  <name>Preset apply endpoint (POST) with atomic transaction + report</name>
  <files>backend/api/settings.py, tests/test_api_settings_write.py</files>
  <action>
    Add POST /api/projects/{project_id}/presets/{preset_id} to backend/api/settings.py (D-05, D-04 backend half). Handler: depend on get_project; run one synchronous block in asyncio.to_thread: connect, ensure ready, presets.get_preset(conn, preset_id) — catch presets.UnknownPreset and raise HTTPException 404 with the preset id — then presets.apply_preset(conn, preset_id, project_id) inside a caller-owned transaction: commit on success, rollback on any exception (apply_preset itself never commits, so a failure must leave the project untouched), close. Return {"report": report, "values": store.resolve_all(conn, project_id=project_id)}.

    The handler must not decide anything about the preset payloads itself — applying is entirely presets.apply_preset's behaviour: overrides only, clears overrides that agree with defaults, skips DFM keys whose value is not an available option on this machine and reports them in "unavailable" (do not treat an empty dfm_models directory as an error; the report carries it).

    Extend tests/test_api_settings_write.py: resolve preset ids by name through GET /api/presets rather than hardcoding; apply preset A -> 200, report has a non-negative written count, and values now equal the preset's non-default project values for at least one key the preset changes; apply preset with AUD likewise; applying the same preset twice succeeds both times (idempotent, no exception); an unknown preset id answers 404; the "unavailable" list is present and is a list (it may be empty or name DFM keys depending on the machine's model assets); a global-tier key carried by the preset lands in the global tier (assert via the values map). These tests enter the app lifespan, so they inherit the Phase 4 model bootstrap gate — run them on `.venv-clean` with `MODELS_VERIFY_MODE=fast` (and `MODELS_DIR` to a complete set where the owned copy is absent), the same fixture rule as plan 01. "No settings value depends on a model file" is true; "the app starts without models" is **not** — the gate refuses to start, so a complete model set must be reachable.
  </action>
  <verify>
    <automated>.venv-clean/Scripts/python.exe -m pytest tests/test_api_settings_write.py -x</automated>
  </verify>
  <done>Both migrated presets apply correctly through the API, the report shape is stable, unknown presets answer 404, and apply is atomic and repeatable. The tests run on the `.venv-clean` interpreter against a complete model set (the Phase 4 startup gate requires models present).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> PUT /api/projects/{id}/settings | Untrusted {key: value} pairs cross here; every value must pass the Phase 3 validator before anything is stored |
| client -> POST /api/projects/{id}/presets/{preset} | Untrusted preset id crosses here; existence is checked before any write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | PUT settings overrides body | high | mitigate | Every key/value passes store.set_project -> validate() (type, bounds, options) and _check_key (tier); wrong tier / unknown key / invalid value -> 400 with rollback, nothing partially persisted; project_id validated by get_project |
| T-05-06 | Tampering | POST preset apply | medium | mitigate | preset_id resolved via presets.get_preset (404 on unknown) before any write; apply_preset runs in a caller-owned transaction committed only on success |
| T-05-07 | Spoofing | settings writes via the API | low | accept | Single-user local tool bound to localhost by default (config.py host 0.0.0.0 is the deploy surface; CORS restricts browser origins); no auth is in scope (PROJECT.md out-of-scope: multi-user/auth/hosting) |
</threat_model>

<verification>
- .venv-clean/Scripts/python.exe -m pytest tests/test_api_settings_write.py -x passes.
- PUT round-trip: change -> persist -> fresh GET returns the change (FRONTEND-01 criterion 3 backend half).
- Rejection matrix: unknown key / wrong tier / invalid value / mixed body -> 400 with zero partial writes.
- Preset apply: both presets, atomic, 404 on unknown, report stable.
</verification>

<success_criteria>
- The D-05 write endpoints exist with pydantic models and the plan 01 error-mapping helper.
- Only provided overrides are persisted (D-02 backend half); invalid input stores nothing.
- Preset application is atomic and repeatable, with a report the frontend diff modal can surface.
</success_criteria>

<output>
Create `.planning/phases/05-frontend-schema-rendering/05-02-SUMMARY.md` when done
</output>
