---
context: phase
phase: 05.1-studio-frontend-media-workspace
task: 0
total_tasks: 0
status: paused
last_updated: 2026-08-23T20:40:01.800Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: delegation-outage — harness background-agent spawning (`subagent`, `workflow` agent(), `subagent_fork`) failed a trivial "reply OK" probe on 3 consecutive goal rounds (2026-08-23). GSD plan-phase REQUIRES independent planner/checker contexts; collapsing roles inline is forbidden without explicit user override. Structural mitigation required: re-probe with a trivial subagent call FIRST; only spawn real roles after a success; if still failing, report — do not degrade.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| delegation-outage workarounds | Attempting orchestrator-direct planning when subagents fail | blocking | Trivial-probe gate before every role spawn; staged prompt file makes retry deterministic |
| PowerShell JSON arg mangling | `{\"k\":\"v\"}` corrupts node CLI JSON args | advisory | Use single-quoted `'{"k":"v"}'` for gsd-tools JSON args |
| state.* handler corruption | state.patch/rebuild/planned-phase have corrupted STATE.md frontmatter before | advisory | After any state.* call, diff STATE.md and surgically repair instead of re-running handlers |
| GPU-contaminated benchmarks | First bench run was 1.4-3.9x slow from GPU contention | advisory | All perf gates REQUIRE idle GPU; note contention in docs/benchmark-baseline.md |

<current_state>
Phase 05.1 (Studio Frontend Media Workspace) is decision-complete but PRE-PLANNING. The
plan-phase workflow ran its init cleanly (research legitimately skipped via config,
hooks resolved, capability flags checked), then hit the harness delegation outage at the
pattern-mapper spawn. The FULLY-COMPOSED planner prompt is staged on disk at
`.planning/phases/05.1-studio-frontend-media-workspace/05.1-PLANNER-PROMPT.staged.md` —
it encodes every binding constraint (D-01..D-15, benchmark sequencing, quality gates,
return markers) so the next session can spawn instantly.
</current_state>

<completed_work>

- Benchmark mandate EXCEEDED: two-tier engine bench + headless VisoMaster comparison +
  lever matrix (embed-cache +52% -> 10.3fps floor; threads dead lever; upstream itself
  8.26fps). Commits 8536de3, 32e1468, f4cde9a. Authoritative doc:
  docs/benchmark-baseline.md (includes Decision recorded + Session addendum sections).
- Discuss-phase complete earlier: CONTEXT D-01..D-15 + Canonical References
  (predecessor recorder._export/_stem save-on-stop spec). Commits effa4bf, 48cfca2.
- ROADMAP Phase 05.1 inserted, STATE current_phase=05.1 (commits 8f25161, f12e92a).
- /gsd-progress routed correctly to plan-phase; init.plan-phase validated everything.
- Planner prompt composed and staged to disk (see current_state).
</completed_work>

<remaining_work>

- Re-probe delegation; on success run pattern-mapper (optional/non-blocking)
- Spawn gsd-planner with the staged prompt content (delete staged file after spawn)
- Handle planner return markers (PLANNING COMPLETE expected; filesystem fallback via
  find-phase if marker lost — known Windows stdio hang pattern)
- gsd-plan-checker loop, max 3 iterations
- Gate 13a decision coverage (BLOCKING - all D-01..D-15 must be cited in plans),
  13b state.planned-phase, 13c roadmap.annotate-dependencies, 13d commit plans
- Then execute all plans; gates green (backend suite .venv-clean MODELS_VERIFY_MODE=fast,
  vitest incl. 201-control DOM-count gate, npm build, Playwright E2E); SUMMARYs;
  STATE/ROADMAP updates; goal complete
</remaining_work>

<decisions_made>

- Embedding cache = Plan 01 with idle-GPU re-bench gate (measured +52%; mandated by
  baseline doc "Decision recorded")
- Nearest-previous-swapped-frame overlay IS the design (proven by benchmark evidence)
- Save-on-stop takes ported from predecessor recorder semantics ({project}_{stamp}_{face}.mp4[.partial],
  accumulate forever, working file wiped per start, export never fatal)
- Threads exposed but no perf claims (measured dead lever)
- Research skipped in plan-phase (config research=false); Nyquist artifacts not required
- User chose wait-and-retry over orchestrator-direct fallback during outage
</decisions_made>

<blockers>
- Harness background-agent delegation down (all 3 transports fail trivial probe):
  status external/transient; workaround = probe-gate then resume from staged prompt.
  User informed each round; explicitly declined inline-planning override.
</blockers>

## Required Reading (in order)

1. `.planning/phases/05.1-studio-frontend-media-workspace/05.1-PLANNER-PROMPT.staged.md` — the exact planner spawn input; also restates every binding constraint
2. `.planning/phases/05.1-studio-frontend-media-workspace/05.1-CONTEXT.md` — decisions D-01..D-15 + Canonical References (BINDING on plans)
3. `docs/benchmark-baseline.md` — authoritative perf evidence; Plan-01 sequencing mandate lives here
4. `.planning/ROADMAP.md` Phase 05.1 section — authoritative scope prose
5. `D:/Visomaster/webui2/{index.html,app.js,styles.css}` — READ-ONLY look-and-feel reference (dark tokens, card grid)

## Critical Anti-Patterns (do NOT repeat these)

- COLLAPSING PLAN ROLES INLINE when delegation fails → probe-gate + report instead (GSD contract)
- Reading agent contract files into ORCHESTRATOR context → subagents read their own contracts
- Trusting STATE.md after state.* handler calls → diff immediately, repair surgically
- Benchmarks on busy GPU → idle-only discipline, all numbers recorded in baseline doc

## Infrastructure State

- Dev server: uvicorn :8000 running as background job pwsh-28 (serves built dist; StaticFiles from disk) — offered stop earlier, user never asked
- Sealed-runner interpreter: .venv-clean/Scripts/python.exe (torch+Qt); tests arm seal via tests/_engine_runner.py
- Bench tools: tools/benchmark_engine.py (--micro [--threads N] [--embed-once], --pipeline [N]); tools/benchmark_visomaster.py (runs ON D:\Visomaster\dependencies\Python\python.exe, cwd=tools/.vmbench junction scratch)
- Long personal clip for pipeline benches: D:\Visomaster\web_uploads\02d96dc9742e\target.mp4 (720p25, 4141 frames); faces in D:\Visomaster\inputt\*
- D:\Visomaster is READ-ONLY (broken .git) — never run git ops there
- Goal tool: goal-2eee7b30 active, round 3/12, NOT marked blocked (user chose wait-and-retry)

<context>
Everything upstream of planning is done and evidenced. The ONLY wall is infrastructure.
The staged planner prompt means zero recomposition work: probe, spawn, verify markers,
run checker, pass gates, commit. If delegation recovers mid-session even once, the whole
planning chain (~10 min of agent time) completes in one go.
</context>

<next_action>
Start with: trivial subagent probe ("Reply with exactly: OK", run_in_background false).
On success -> read 05.1-PLANNER-PROMPT.staged.md and spawn gsd-planner with exactly that
content. On failure -> report round status; do NOT plan inline without explicit fresh
user override.
</next_action>
