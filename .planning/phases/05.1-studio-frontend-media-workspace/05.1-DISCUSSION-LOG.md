# Phase 05.1: Studio Frontend Media Workspace - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-23
**Phase:** 05.1-studio-frontend-media-workspace
**Areas discussed:** App shell & routing; Face workflow; Playback & export depth; Periphery scope cuts; Benchmark methodology; Generation mode defaults; Face deletion rules

---

## Prologue: how this phase exists

The session began as `/gsd-resume-work` after Phase 5 closure. Status was presented; the user was offered Phase 6 discussion options but redirected: *"Complete frontend with Fixes to the 201 options, check the webui I built in the D:\visomaster for reference as to how it might look."* The reference was located (`D:/Visomaster/webui` v1, `D:/Visomaster/webui2` with gallery) and the user confirmed **webui2** ("the one that which lives at /studio endpoint" — its assets were served under `/studio/`). Routing decision: insert a new phase into milestone v1.0 rather than verify first — executed via `phase.insert` as **Phase 05.1** (decimal, ordered before verification) to avoid renumbering every existing "Phase 6 = End-to-End Verification" reference.

## App shell & routing

| Option | Description | Selected |
|--------|-------------|----------|
| One page at / | Player + media + faces + Controls card (201 settings inside) on ONE page, faithful to webui2 | ✓ |
| /studio + / split | Workspace at /studio like the old setup; today's settings app stays at / untouched | |
| / workspace + /settings | Workspace is home; settings moves whole to /settings | |

**User's choice:** One page at /
**Notes:** User initially floated "/studio endpoint" when identifying the reference build; chose single-page once the tradeoff was explicit.

| Option | Description | Selected |
|--------|-------------|----------|
| Tab per schema group | Each schema group becomes a tab across the Controls card | |
| Keep sidebar, add tabs | Today's mini-sidebar + scrolling groups stay as-is; tabs only for non-settings content (Jobs etc.) | ✓ |
| Five Studio tabs | Reproduce Face Swap/Common/Face Editor/Settings/Jobs tabs and distribute groups underneath | |

**User's choice:** Keep sidebar, add tabs

## Face workflow

| Option | Description | Selected |
|--------|-------------|----------|
| Saved-faces strip | Thumbnail strip, upload several, click one to activate; new endpoints, stored per project (as first framed) | ✓ |
| Single active face only | Upload replaces the one face; no library | |

**User's choice:** Saved-faces strip — then scoped GLOBAL, not per-project:

| Option | Description | Selected |
|--------|-------------|----------|
| Per-project library | Faces stored under each project's cache dir | |
| Global face library | One machine-wide collection shared by all projects, like the old `inputt` folder | ✓ |

| Option | Description | Selected |
|--------|-------------|----------|
| Automatic targeting | Engine keeps deterministic female-first/largest choice | ✓ "1 for now, 2 later planned." |
| Manual pick from detections | Click which detected face gets the swap; needs detect-with-boxes + engine-seam target index | planned later |

**Notes:** Manual picking is *planned follow-up work*, recorded in CONTEXT D-04 so planning doesn't foreclose it.

## Playback & export depth

| Option | Description | Selected |
|--------|-------------|----------|
| Full live overlay | Swapped frames paint over playing video behind playhead, ws-driven; predecessor's defining behavior | ✓ "Both Options. Priority 1." |
| Export-first | Preview button + Record Range produce output to watch afterwards; no continuous overlay | also wanted |

**User's choice:** Both, live overlay priority 1.

| Option | Description | Selected |
|--------|-------------|----------|
| Range + full video | Set Start/Set End transport buttons + full-video toggle (backend fields exist) | ✓ |
| Full video only | Whole video exports only | |

| Option | Description | Selected |
|--------|-------------|----------|
| Manual preview button | One-shot preview endpoint, renders frame at playhead | |
| Skip preview | Output visible only via overlay/export | |
| Button + auto-preview | Button plus auto re-render on setting/face changes while paused (Studio parity) | ✓ |

## Periphery scope cuts

| Option | Description | Selected |
|--------|-------------|----------|
| Jobs tab | Current/all jobs from frame status + ws | ✓ |
| Final Results card | `.part` while recording + finished mp4 + download | ✓ |
| Video Library grid | Watched-folder grid like old /api/library | deferred |
| Gallery page | Page of past exports; needs export-history persistence | ✓ |

**User's choice (free text):** "1, 2, 4. also another feature request I need is that It should save partial generated … note it down. also at the end of this discussion, before starting work, you need to benchmark"
**Notes:** "Save partial generated" recorded verbatim in CONTEXT D-12/deferred for planning interpretation. Benchmark mandate became D-11.

## Benchmark methodology

| Option | Description | Selected |
|--------|-------------|----------|
| Two tiers | Tier 1 engine micro (fps/latency, source res + 1080p, CUDA, sealed runner); Tier 2 pipeline stream-mode generated frames/min vs video fps → docs baseline | ✓ |
| Engine micro only | Quick fps numbers without scheduling layer | |
| Pipeline only | Only the overlay-decision number | |

## Generation mode defaults

**User's choice (free text):** "3 modes, stream live is default, other 2 option is range/full export and interval mode with n sec."
**Notes:** Captured as D-07: three explicit modes — Stream-Live (default), Export (range/full), Interval (N sec).

## Face deletion rules

| Option | Description | Selected |
|--------|-------------|----------|
| Warn + cascade | Dialog lists affected projects; confirm deletes file; projects fall back to no-source state | ✓ |
| Block while in use | Refuse delete until deactivated everywhere | |
| Free delete | Always succeeds; missing-path errors surface at generate-time | |

## the agent's Discretion

Overlay rendering mechanism; appbar composition/status pill; empty states; auto-preview debouncing; face-store layout/thumbnail sizing; benchmark clip selection.

## Deferred Ideas

Video Library grid · manual target-face picking (planned next) · save-partial-generated policy (planning decision) · Restart Engine button (concept N/A with in-process engine) · multi-face swapping (permanently out of scope).
