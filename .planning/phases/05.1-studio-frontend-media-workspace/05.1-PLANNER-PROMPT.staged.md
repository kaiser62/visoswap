<!-- STAGED PLANNER PROMPT for Phase 05.1 — orchestrator composes the spawn from this file.
     Delegation outage 2026-08-23 (subagent/workflow-agent/fork all failing). Delete after successful planner spawn. -->

You are the gsd-planner agent for project VisoSwap. Repo root: D:\Dev\visoswap (Windows).

FIRST read your behavioral contract at D:\Dev\visoswap\.agents\agents\gsd-planner.md and follow it exactly, including its Universal Anti-Pattern rules (never read files into your reply; agents read files from disk themselves; no placeholders; concrete identifiers only).

<planning_context>
**Phase:** 05.1 — Studio Frontend Media Workspace
**Mode:** standard (granularity: medium, tracer-first decomposition ON — lead with ONE production-quality end-to-end vertical slice, verify it, then expand)

<required_reading>
- .planning/STATE.md (Project State)
- .planning/ROADMAP.md (Phase 05.1 goal section — the authoritative scope prose)
- .planning/REQUIREMENTS.md (background)
- .planning/phases/05.1-studio-frontend-media-workspace/05.1-CONTEXT.md (USER DECISIONS D-01..D-15 + Canonical References — BINDING)
- docs/benchmark-baseline.md (AUTHORITATIVE perf evidence + "Decision recorded" + "Session addendum" sequencing mandates)
- If 05.1-PATTERNS.md exists in the phase dir, read it as your analog map; if not, find analogs yourself.
</required_reading>

No RESEARCH.md exists (research disabled) — ground every plan in the actual code: read backend/api/*, backend/services/{scheduler,recorder,cache,engine}.py, backend/engine_backend.py (or its real path), frontend/src/* (schema-driven Controls from Phase 5), tests/ conventions (sealed runner, _engine_runner.py, fixtures/engine_settings.json), and the read-only reference D:/Visomaster/webui2/{index.html,app.js,styles.css}.

**Output:** .planning/phases/05.1-studio-frontend-media-workspace/05.1-01-PLAN.md, 05.1-02-PLAN.md, … (zero-padded). Each plan: YAML frontmatter (plan id, title, depends_on, waves, must_haves with truths/prohibitions, requirements_addressed may be omitted — phase_req_ids is None), <objective>, <read_first>, ordered <task> blocks each with <action> (concrete file paths + identifiers, NO fenced code), <acceptance_criteria> (behavior/test-command/CLI/source assertions), <read_after>; end with "Artifacts this phase produces" listing every new symbol/file path.
</planning_context>

MANDATORY SEQUENCING (from benchmark evidence, non-negotiable):
- Plan 01 = source-embedding cache in the engine seam (keyed by resolved path+mtime+size, invalidated on load()) + idle-GPU re-benchmark gate reusing tools/benchmark_engine.py (--embed-once projection already measured 96.9ms/10.3fps; the gate asserts the real cache lands within noise of that projection) + scheduler hot-swap verification task (stop→change face→start purity: per-start wipe already exists; verify generator freshness + decide purge-on-stop) + threads finding honored (nThreadsSlider stays exposed, no perf claims, D-14).
- Later plans: shell/layout → faces → playback/export → periphery, per D-01..D-15.

BINDING CONSTRAINTS:
- One-page app at `/`; Controls card keeps sidebar+group layout with ALL 201 controls rendered (DOM-count vitest gate must survive); tabs only for non-settings surfaces (D-01/D-02).
- Global face library: upload/list/delete/activate endpoints; delete warns + cascades assignments; thumbnails (D-03). Automatic face targeting now; manual per-target picking is a PLANNED FOLLOW-UP phase — do not foreclose it in API shapes (D-04).
- Live ws overlay P1: nearest-previous-swapped-frame behind playhead (proven correct by benchmark — no config reaches playback today); ws is informational-only, reconnect refetches frames (D-06).
- Three modes: Stream-Live (default) / Export range+full / Interval-N (D-05).
- Save-on-stop takes: port predecessor recorder._export/_stem semantics verbatim in spirit — every run close (clean OR stop) exports {project}_{YYYYMMDD-HHMMSS}_{face}.mp4[.partial] to an output folder; takes ACCUMULATE; next start wipes only working output.mp4(.part); export failure never fails the run (D-12 + Canonical References in CONTEXT).
- Jobs tab shows generated-vs-played ratio honestly (D-15); Final Results card is .part-aware; Gallery page lists exported takes.
- Video Library grid DEFERRED. Restart Engine button N/A (D-10 area).
- Path validation stays at backend boundary (T-02-07); TensorRT stays refused (T-02-09); processing_scale/width exposure is a plan task with bench gate (D-15b).
- Errors map via router patterns; preview button + auto-preview-while-paused (D-07 area).

<quality_gate>
- [ ] Phase goal fully covered by plans; every D-01..D-15 decision cited in ≥1 plan's must_haves/truths (decision-coverage gate is BLOCKING)
- [ ] Every task has <read_first> and <acceptance_criteria>; actions contain concrete identifiers, no fenced code
- [ ] Dependencies + waves assigned for parallel execution
- [ ] must_haves derived from phase goal; "Artifacts this phase produces" present
- [ ] No placeholders, no TODOs, no invented APIs — verify every referenced symbol by reading the real file
</quality_gate>

End your reply with exactly one marker:
## PLANNING COMPLETE
then the plan file list + one-line-per-plan summary.
(Alternative markers if truly blocked: ## PHASE SPLIT RECOMMENDED / ## CHECKPOINT REACHED / ## PLANNING INCONCLUSIVE)
