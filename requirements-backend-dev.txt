# Ported from config-parallel-setup/requirements-dev.txt.
pytest==8.3.4
pytest-asyncio==0.25.2
asgi-lifespan==2.1.0
ruff==0.8.6
# FastAPI TestClient requires httpx (plan 05-01). Loose pin; already part of the
# web stack installed into .venv-clean.
httpx>=0.27,<1
