"""Three-tier settings storage and resolution.

``db.py`` holds the DDL; ``store.py`` holds the reads and writes. Both are
standard library only and neither imports anything outside ``visoswap.schema``.
"""
